services:
  - type: static
    name: phone-store-demo
    buildCommand: echo "No build required for static site"
    staticPublishPath: ./
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
